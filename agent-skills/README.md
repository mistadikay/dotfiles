# Agent Skills

Vendored personal agent skills.

Run `~/.dotfiles/agent-skills/install.sh` to symlink these skills into:

- `~/.claude/skills` for Claude Code
- `~/.codex/skills` for Codex

OpenCode discovers `~/.claude/skills`, so the default install makes these skills
available to OpenCode without creating duplicate entries.
