# Copy to ~/.config/agents/secrets.env and keep the real file out of git.
#
# The sync script also reads existing local harness configs and preserves
# already-rendered header values when possible, but this file is the clean
# source for new machines.

CONTEXT7_API_KEY=
NIA_API_KEY=
